import numpy as np
import os, json, sys
import pandas as pd 

def createVariableNodes( file_, mode='LOCAL' ):
    ## only reads csv format inputs .. TBD - for diff input types
    df = pd.read_csv( file_ )

def createMethodNodes( file_, mode='LOCAL' ):
    ## only reads csv format inputs .. TBD - for diff input types
    df = pd.read_csv( file_ )

def createRelations( file_ ):
    ## only reads csv format inputs .. TBD - for diff input types
    df = pd.read_csv( file_ )
     
